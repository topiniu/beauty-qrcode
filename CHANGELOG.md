# Changelog

## 0.2.0

- Added SVG rendering through `renderer="svg"` while keeping the DOM renderer as default.
- Standardized the package on named exports.
- Moved published type declarations to `dist/`.
- Tightened npm package metadata, dependency boundaries, and release validation scripts.
- Updated documentation and release workflows for an npm-focused open-source package.
